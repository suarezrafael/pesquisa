name = "missao-aprender-accounts"
main = "src/index.ts"
compatibility_date = "2026-08-22"
compatibility_flags = ["nodejs_compat"]

# DATABASE_URL (string de conexão do Neon) é um secret, não uma var comum — nunca comitado.
# Local: app/server-accounts/.dev.vars (gitignored). Produção: `wrangler secret put DATABASE_URL`.
