# Laboratório atual

Último concluído: labs/lab-44-scatter-geral-e-gatos-flutuando/ (o fix do lab-43 (raycast de chão
real, `terrainGroundRadial`) só tinha sido aplicado em escolas/torre/rochas de montanha; estendido
pro scatter geral de props, scatter do deserto, loja e gatos empoleirados; verificação ao vivo
EXAUSTIVA — todos os objetos, não uma amostra — confirmou zero casos reais de flutuação)
Contexto para o próximo laboratório: labs/lab-44-scatter-geral-e-gatos-flutuando/CONTEXT.md

Trabalho acontece numa branch de worktree (`worktree-abstract-wobbling-owl`), a partir de `main`
(ainda não mesclada — esta sessão não pode mesclar em main; ver comando de merge/PR em
`labs/lab-44-scatter-geral-e-gatos-flutuando/CONTEXT.md`, seção "Estado do repositório ao final").

Nenhum pedido novo pendente. Se o usuário ainda relatar objetos flutuando depois deste
laboratório (mesmo após um hard refresh do navegador), a verificação exaustiva não encontrou
nenhum caso real — vale pedir uma screenshot de perto com o nome/posição do objeto pra investigar
mais a fundo (ver `labs/lab-44-scatter-geral-e-gatos-flutuando/CONTEXT.md`).

Para retomar o trabalho numa nova sessão, leia primeiro
`labs/lab-44-scatter-geral-e-gatos-flutuando/CONTEXT.md` (o que foi feito e o que vem a seguir) e
rode a skill `lab` no modo `start` pra abrir o próximo laboratório.
