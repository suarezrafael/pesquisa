# Laboratório atual

Último concluído: labs/lab-36-parkour-espiral/ (terceiro desafio de parkour — torre em espiral,
12 degraus, 1,5 volta, sobe ~9,35 unidades, 6 moedas no topo)
Contexto para o próximo laboratório: labs/lab-36-parkour-espiral/CONTEXT.md

Trabalho acontece numa branch de worktree (`worktree-abstract-wobbling-owl`), a partir de `main`
(ainda não mesclada — usuário pediu merge, mas esta sessão não pode mesclar em main; ver comando
de merge/PR em `labs/lab-36-parkour-espiral/CONTEXT.md`, seção "Estado do repositório ao final").

Toda a fila de pedidos abertos desde o lab-32 (montanhas, rio removido, mixagem de áudio,
velocidade, prédio navegável, mini-game de bichos, terceiro parkour) está concluída. Nenhum
pedido novo pendente — o próximo laboratório deve perguntar o que fazer a seguir, ou considerar
fortemente uma sessão de PLAYTESTING REAL (jogando de verdade) de tudo construído nos labs 31-36,
já que toda a verificação até agora foi por raycast/build/inspeção de cena, nunca por movimento
real do jogador.

Para retomar o trabalho numa nova sessão, leia primeiro `labs/lab-36-parkour-espiral/CONTEXT.md`
(o que foi feito e o que vem a seguir) e rode a skill `lab` no modo `start` pra abrir o próximo
laboratório.
