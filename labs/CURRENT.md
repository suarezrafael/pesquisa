# Laboratório atual

Último concluído: labs/lab-31-rio-raycast-filtrado/ (raycast do rio agora filtra colisores que não
são o planeta — avatar/bichos/árvores não contaminam mais a altura da água — e a folga acima do
chão real subiu de 0,03 pra 0,15 pra não depender de sorte no depth buffer)
Contexto para o próximo laboratório: labs/lab-31-rio-raycast-filtrado/CONTEXT.md

Trabalho acontece numa branch de worktree (`worktree-abstract-wobbling-owl`), a partir de `main`
(ainda não mesclada — usuário pediu merge, mas esta sessão não pode mesclar em main; ver comando
de merge/PR em `labs/lab-31-rio-raycast-filtrado/CONTEXT.md`, seção "Estado do repositório ao
final").

Pedidos novos do usuário já na fila para o próximo laboratório (ver
`labs/lab-31-rio-raycast-filtrado/CONTEXT.md`, seção "O que o próximo laboratório deve
desenvolver"): montanhas maiores/mais casas em cima delas com colisão, mixagem de áudio (tirar
música, manter som ambiente), novo desafio de parkour mais alto com mais moedas no topo.

Para retomar o trabalho numa nova sessão, leia primeiro `labs/lab-31-rio-raycast-filtrado/CONTEXT.md`
(o que foi feito e o que vem a seguir) e rode a skill `lab` no modo `start` pra abrir o próximo
laboratório.
