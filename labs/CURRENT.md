# Laboratório atual

Último concluído: labs/lab-42-rochas-montanhas-e-regressao-critica/ (rochas de verdade em cada uma
das 12 montanhas — 48 no total —, e uma regressão crítica encontrada e corrigida no processo: um
ReferenceError não tratado que interrompia toda a inicialização da cena depois das montanhas)
Contexto para o próximo laboratório: labs/lab-42-rochas-montanhas-e-regressao-critica/CONTEXT.md

Trabalho acontece numa branch de worktree (`worktree-abstract-wobbling-owl`), a partir de `main`
(ainda não mesclada — usuário pediu merge, mas esta sessão não pode mesclar em main; ver comando
de merge/PR em `labs/lab-42-rochas-montanhas-e-regressao-critica/CONTEXT.md`, seção "Estado do
repositório ao final").

Nenhum pedido novo pendente. **Atenção**: este laboratório encontrou um bug crítico (regressão
que quebrava toda a inicialização da cena) que só apareceu jogando de verdade, não no `npm run
build` — reforça a recomendação de PLAYTESTING REAL antes de considerar qualquer lote de mudanças
"pronto", mesmo quando o build passa limpo.

Para retomar o trabalho numa nova sessão, leia primeiro
`labs/lab-42-rochas-montanhas-e-regressao-critica/CONTEXT.md` (o que foi feito e o que vem a
seguir) e rode a skill `lab` no modo `start` pra abrir o próximo laboratório.
