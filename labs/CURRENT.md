# Laboratório atual

Último concluído: labs/lab-27-ajustes-carro/ (rua maior e orgânica, boneco visível em cima do
carro ao dirigir)
Contexto para o próximo laboratório: labs/lab-27-ajustes-carro/CONTEXT.md

Trabalho acontece numa branch de worktree (`worktree-abstract-wobbling-owl`), a partir de `main`
(ainda não mesclada — usuário pediu merge, mas esta sessão não pode mesclar em main; ver comando
de merge/PR em `labs/lab-27-ajustes-carro/CONTEXT.md`, seção "Estado do repositório ao final").

Para retomar o trabalho numa nova sessão, leia primeiro `labs/lab-27-ajustes-carro/CONTEXT.md`
(o que foi feito e o que vem a seguir) e rode a skill `lab` no modo `start` pra abrir o próximo
laboratório.
